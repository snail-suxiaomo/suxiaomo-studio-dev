# 苏小沫工作台 更新日志

> 按版本号追加记录。版本号定义在 `desktop/package.json`（打包前手动修改，当前 0.0.6）。本文件为开发侧维护文档，不随安装包分发。
> 约定：代码改动随打包自动带入；数据库不打包，数据靠「复制 workspace 目录」或用户首次启动在设置页自选数据目录（pathMode:custom）迁移。

## v0.0.6（2026-08-12 ~ 08-13，待打包前手动改 package.json 版本号）
【AI 密钥库（独立模块）· 模型配置联动密钥库】
- 新增独立模块「AI 密钥库」（与模型配置严格区分命名：后端 backend/key_vault/ 对 backend/model_config/；前端 frontend/src/key_vault/KeyVault.vue 对 frontend/src/model_config/ModelConfig.vue）：集中管理各厂商 / 平台 / 账号 / 密钥 / 额度 / 过期，避免密钥散落在每条模型配置里。
  - 表 key_vault（bundled/schema/key_vault.sql）：name / provider / category(文本大模型·图像生成·视频生成·语音合成·翻译·其他) / base_url / api_key / secret_key / account / dev_url / doc_url / quota / expire_at / status(正常·即将过期·已过期·已停用) / tags / note / sort_order
  - 后端 service.py/router.py：CRUD + 按 category/keyword 筛选 + 批量删除 + Excel 导入导出（openpyxl，列名智能识别同义词，导入策略 skip/overwrite/new）；导出含 api_key/secret_key 明文（已在 UI 提示妥善保管）
  - 前端 KeyVault.vue：表格列表 + 增删改查 + 管理态多选(全选/导出选中/批量删除) + 导入弹窗 + 过期自动标记(30 天黄/已过红) + 密钥掩码显示/一键复制明文 + 左侧侧边栏新入口「🔑 AI 密钥库」
- 模型配置联动密钥库：model_config 加 key_vault_id 列（db.py 迁移，幂等）；ModelConfig.vue 新增「关联密钥库」下拉，选后自动带出 base_url + api_key（表单内掩码、不可手改），保存时后端以密钥库为准再同步一次（common/ai.py 读取逻辑不动，老数据兼容）；密钥库 key 变更后重新保存模型配置即自动同步最新 key

【每日任务：积分字段（累加 / 每日清空）· 2026-08-13 新增】
- 新增两列 `points`（积分数，非负整数）与 `points_mode`（枚举 `cumulative` 累加永久可用 / `daily` 每日领取当日清空），仅标记+展示，不建余额表。
- 列表卡片新增积分徽标（+N 积分 + 清零方式标签）；顶部「今日可领积分 / 累计积分(永久)」统计卡经反馈后已移除，仅保留每张卡片上的积分展示。
- 新增 `GET /api/daily-tasks/points-summary`（仅供后端推导验证，前端当前未使用）。
- 兼容旧库：`ensure_columns` 自动 ALTER 增补两列；改后导入顺序为先 `drop_required_accounts` 再 `ensure_columns`，两条迁移互不干扰。
- 仅后端 daily_tasks（schema/service/router）+ 前端 DailyTasks.vue 改动；路由/页面文件名不变。
- ✅ 苏小沫自测通过（2026-08-13）：积分字段新增/编辑、卡片徽标展示均正常。

【侧边栏重构 · 2026-08-13】
- 顶层顺序重排 + 改名（仅改 frontend/src/App.vue，路由/页面/后端不变）：
  - 文件管理(原文件空间) / 应用中心(原应用启动器) / AI指令库(原prompt库指令) / 模型API(原AI密钥库) / 羊毛管理(原免费资源) / 推文助手(原小说推文)
  - 提示词库从顶层移回「漫剧创作」子菜单并置顶；账号矩阵提到每日任务与漫剧创作之间的顶层。
  - 顶层最终顺序：首页 · 每日任务 · 文件管理 · 爆款收集 · 漫剧创作(分组) · 账号矩阵 · 应用中心 · 网址导航 · AI指令库 · AI助手 · 模型API · 模型配置 · 设置 · 日志查看(dev) · 发布版本(dev)
- 漫剧创作子菜单(7项)：提示词库 · 小说创作(开发中) · 剧本创作(开发中) · 小说转漫剧(重构中) · 小说指令配置(重构中) · 羊毛管理 · 推文助手

【账号矩阵：修复接口 404 + 新增导入模板 · 2026-08-13】
- 根因（真正导致"保存无反应 / 下载模板 404"）：`social_account/router.py` 用裸 `APIRouter()` 且所有路由写成全路径 `/social-accounts/...`（缺 `/api` 前缀）。其他新模块（daily_tasks/key_vault/prompt_library）均用 `APIRouter(prefix='/api/...')`；而 Electron 运行环境会把前端 `/api/...` 原样转发到后端，后端无 `/api` 前缀路由 → 全部 404（不仅是模板，连新建保存 / 列表也是 404）。
- 修复：`social_account/router.py` 所有路由 path 加上 `/api` 前缀（`/api/social-accounts`、`/api/social-accounts/template`、`/export`、`/import`、`/upload-image`、`/reorder`、`/{pid}`），与前端 `api('/social-accounts')` 及 `fetch('/api/social-accounts/...')` 精确对应。
- 附带：后端 venv 原本缺 `openpyxl`（venv pip 已装 `openpyxl==3.1.5`，import 已验证 OK），这是叠加因素之一，但单独装它不能解决 404（根因是前缀）。
- 新增：前端「下载模板」按钮 + 后端 `export_template()` 导出空 Excel 模板（表头：序号/平台/账号名称/账号ID/UserId/主页链接/简介/性别/生日/所在地/获赞/互关/关注/粉丝/二维码/封面）；导入按表头智能识别列名。
- ⚠️ 改动文件：backend/social_account/router.py、backend/social_account/service.py、frontend/src/social_account/SocialAccount.vue。修复后需**重启后端**（带 `SUXIAOMO_DATA_DIR=F:/suxiaomo-studio-workspace`）加载新代码。
- 新增「下载模板」按钮（`GET /api/social-accounts/template`），导出空 Excel 模板，表头含所有支持字段，降低用户手动整理表格的门槛。
- 导入逻辑保持智能列名识别（支持中英文同义词），但表头完全无关时会明确报错。

【文件管理·文件夹快捷入口：彻底去除层级判断，支持任意层级 · 2026-08-15 重做】
- 推倒上一版"2 层 + 超层提示"方案（判断"相对书签根的目录深度"导致深层书签根被误判为第 3 层、首次都生成不了）。本次按苏小沫要求**完全删除层级判断**，支持任意层级、零上限。
- 存储结构 `filespace_roots.pinned_tags` 改为 **路径键字典** `{ 文件夹绝对路径: [该文件夹下的直接子文件夹名,...] }`，例如
  `{ "F:/根": ["A","B"], "F:/根/A": ["A1","A2"], "F:/根/A/A1": ["更深"] }`。
  键即"在哪儿生成的入口"，与磁盘嵌套深度彻底解耦——书签根无论嵌套多深，进去点生成就是第一层；进去它下面的某文件夹点生成就是下一层。
- 生成：在任意文件夹点「扫描本文件夹子目录」→ 后端 `POST /generate-tags { folder_path }` 扫描该文件夹的直接子文件夹，写 `pinned_tags[folder_path]`（只覆盖这一个键，其他键不动）。**无层级校验、无上限**。
- 显示：可折叠树（手风琴）。从路径字典现场重建树，默认展开"当前路径"所在链，其余收起；点 `▸/▾` 展开；点节点进文件夹；`✕` 删除该入口及其下整条分支（级联删子路径键）。
- 命名（按反馈）：区块标题「常用标签」→「文件夹快捷入口」；生成按钮「生成常用文件夹标签」→「＋ 扫描本文件夹子目录」/ 已生成时「重新扫描」；每个节点前加文件夹 SVG 图标区分"是文件夹入口而非搜索词"；「清空」hover 提示"清空本目录所有已生成的快捷入口"。
- 兼容旧数据：新增 `_normalize_tags(raw, root_path)` 在读取时归一化——旧数组 `[a,b]` → `{root:[a,b]}`；旧 2 层 map `{一级:[二级]}`（键为相对根的一级名）→ 展开为路径键字典；新格式（键已是绝对路径）→ 原样返回。写入键保留 root 原有分隔符（Windows 反斜杠），与 `generate_tags` 实写键、前端 `current.value` 完全一致。
- 接口变更：`generate-tags` body 由 `{sub_path}` 改为 `{folder_path}`（必填）；`PUT /tags` body `{tags: <完整路径字典>}`（整体覆盖，供单入口/分支移除、清空后回写）。
- ⚠️ 改动文件：backend/filespace/service.py（重写 generate_tags/set_tags + 新增 _normalize_tags，删 _load_tag_map）+ router.py（GenTagsIn.folder_path / TagsIn.tags）+ frontend/src/filespace/FileSpace.vue（删 depth/isAtRoot/currentLevel1/openTag/openSubTag 整套，改为 tagMap/buildTree/visibleNodes/折叠树模板 + 亮暗色样式）。**后端需重启**（带 `SUXIAOMO_DATA_DIR=F:/suxiaomo-studio-workspace`）加载新接口；前端刷新即生效。
  - 备注：此前"生成标签失败：Failed to fetch"是前端连不上后端（请求路径正确，后端/桌面壳未运行或端口 9100 无响应），与代码改动无关；确保后端在跑即可恢复。
  - 【2026-08-15 热修复】折叠树节点按钮被 `global.css` 第 138 行全局 `button { ... !important }` 渐变主色覆盖，导致节点显示为巨大蓝色长条。在 FileSpace.vue 树节点按钮样式（`.tag-toggle` / `.tag-name`）加 `!important` 覆盖，恢复为透明/浅灰文字按钮。前端 build EXIT=0。
  - 【2026-08-15 热修复 2】折叠树只显示根节点、不显示子节点（如「那年逢明月」下没有 00-原文等）。根因：`buildTree` 用路径键字典匹配子节点时，Windows 反斜杠/正斜杠、大小写差异可能导致 `map[rootPath]` 匹配失败，子节点列表为空；且 `watch(current, expandChain)` 非 immediate，首次进入可能未展开根节点。修正：新增 `normKey()` 统一转小写+正斜杠+去尾分隔符作为匹配键；`buildTree`/`visibleNodes`/`toggleExpand`/`expandChain` 全部用 `normKey`；`expandChain` 始终展开根节点；`watch` 加 `{ immediate: true }`。前端 build EXIT=0。
  - 【2026-08-15 热修复 3】按苏小沫反馈：① 点 ✕ 没反应；② 下拉折叠树看起来别扭；③ 要跟上面的固定分类不在同一行。改为：折叠树 → 当前目录的文件夹快捷入口横向药丸列表；标题行（文件夹快捷入口 + 重新扫描/清空按钮）单独一行，药丸行另起一行；每个药丸显示文件夹图标 + 子文件夹名 + ✕，一行放不下自动换行。同时修复「✕ 没反应」：根因 `removeTag(folderPath, name)` 用 `tagMap.value[folderPath]` 过滤，但 `folderPath` 可能因大小写/斜杠差异与存储 key 对不上，导致 filter 后内容不变、看起来像没反应；改为用 `currentTagKey()` 通过 `normKey` 查找精确存储 key 后再过滤。前端 build EXIT=0。
  - 【2026-08-15 热修复 4】用户反馈标题行（红色框处）仍跟药丸行贴得太紧，要求「换行吧跟上面的通用分类」。修正：`.pin-row` 改为 `flex-wrap: nowrap; width: 100%; margin-bottom: 6px;`，`.pin-label` 加 `margin-right: auto` 顶到左侧，按钮紧跟右侧且不换行；`.pin-chips` 独占下一行。前端 build EXIT=0。刷新即生效，无需重启后端。

【文件管理·常用文件夹标签：修复层级判断（按目录深度误判） · 2026-08-15】
- 根因（苏小沫实测发现）：原判断用「当前目录相对书签根的深度」(depth>=2 禁止)，但书签根本身在磁盘上嵌套很深（如 `F:\AI大杂烩\02-AI相关工具\小说下载器和阅读器`），深度被误算成 3，导致在书签根里点「生成标签」直接弹"第 3 层目录，暂不支持生成"，第一次都生成不了。
- 修正：层级判断**不再依赖目录在磁盘上的绝对/相对深度**，改为「当前路径是否精确匹配已生成标签的位置」：
  - `isAtRoot` = 当前路径 == 书签根路径 → 生成第 1 层标签（扫描当前目录下一级子文件夹）；
  - `currentLevel1` = 当前路径 == 某第 1 层标签对应的完整路径 → 生成该标签下的第 2 层标签（扫描其下一级子文件夹）；
  - 其余位置（第 2 层标签目录 / 任意非标签位置）→ 提示"仅在书签根或已有标签的文件夹下生成（最多 2 层）"。
- 删除 `depth` 深度计数与 `samePath`/`openAddPrefill` 等旧守卫；模板按钮文案由 `depth===1` 切为 `!isAtRoot`。
- 纯前端 FileSpace.vue 改动，后端 `generate_tags(root_id, sub_path)` 逻辑此前已支持（sub_path 为相对书签根的一级名，与 `currentLevel1` 对应），本次无需改后端。
- 前端 build 通过（exit 0，128 模块）。验收：进入任意书签根目录（无论多深）→ 点「＋ 生成常用文件夹标签」正常出第 1 层；点进某一级标签文件夹 → 点「＋ 生成二级标签」出第 2 层；进入第 2 层标签目录再点生成 → 提示"最多 2 层"。

## v0.0.5（2026-08-12）
【提示词库 导出/导入/管理 · 新功能「prompt库指令」 · 提示词内嵌测试】
- 提示词库新增导出/导入/批量管理能力（后端 service.py/router.py + 前端 PromptLibrary.vue）：
  - 导出：自包含 ZIP（prompts.json 全字段含 id+sort_order+scope + prompt_images/ 真实图片 + skills/ 整个文件夹），删后重导展示一致；可选 scope 过滤（各功能各导各的）
  - 导入：支持 .zip/.json，两种模式——restore（保留 id+sort_order 精确还原，用于同库删后重导）/ new（新建防冲突，用于分享）；按 title 去重跳过
  - 管理态：列表顶部「管理」切换 → 卡片复选框 + 工具条（全选/导出选中/批量删除）；导入按钮常驻
  - 一致性经隔离自测验证（文本/图片/skill 三类：导出→删除→重导，全字段与磁盘文件逐字节一致）
- 新功能「prompt库指令」：提示词库加 `scope` 列（prompt/instruction），现有 skills 记录迁移为 instruction；左侧侧边栏新入口 + 重新设计的独立页（frontend/src/prompt_instruction/PromptInstruction.vue），文本指令与技能文件双页签，与原提示词库彻底解耦、各导各的；技能支持上传压缩包 + 打开本地文件夹（后端 /{pid}/open-folder，安全约束限数据根内）
- 提示词详情「测试」按钮（两页均加）：内嵌调用已配置大模型看返回（复用 /chat/ask），实现「看到提示词直接试」；图/视频型当前为文本预览，即梦/可灵真实生成待配置密钥后启用
- db.py 迁移：prompt_library 加 scope 列；打包态/开发态均幂等，老库自动 ALTER 加列并迁移 skills

## v0.0.4（2026-08-09）
【小说推文样式收尾 / 业务查询逻辑优化 / 模型配置修复 / 能力字段落库】
- 小说推文详情弹窗：徽章区（知乎/小程序等）与「推文信息」之间加水平分割线（去掉圆点方案）；右上角关闭按钮改为可见样式（浅灰圆底、hover 红底白字），不再透明不可见；分类常量 DEFAULT_CATEGORIES 更名 FALLBACK_CATEGORIES（正式分类以后端 /meta 为准）
- 小说推文后端逻辑优化（service.py）：①回填统计 _calc_backfill 修复 P0——`if not bf` 改为 `if bf is None`，账号级配置为 0 不再误当「缺失」回退平台级；②list_campaigns 消除 N+1（P1）——压成 2 次查询（一次性取全部 platform 按 campaign_id 内存分组聚合），账号按账号去重，修复 GROUP_CONCAT 跨平台吞账号
- 模型配置 ModelConfig.vue：切厂商时 onProviderChange 无条件同步接口地址 `form.base_url = p.base_url`（新建/编辑共用，不再残留 DeepSeek 地址）；名称必填 + 新建时 API Key 必填（前端校验，不填不让保存）；编辑时 Key 为空从 payload 删除避免清空旧 Key；修复切「自定义/OpenAI 兼容」厂商白屏（currentProfile 未保护访问，加 `currentProfile &&`）
- 模型配置能力字段落库与同步：schema 加 supports_files 列（紧邻 supports_vision）；db.py 迁移 dict 补该列（老库 ALTER 自动加列）；service.py create/update 读写 supports_files；ai.py 无 profile 分支改读库里用户勾选值（原硬编码 0），使 AI 助手按配置能力走；卡片展示 supports_files / supports_vision 标记
- 发布版本页兜底：PackagingView 后端断连时 catch 兜底显示「不可用」（/build/available 不可达即判不可用，避免误报为可用）；排查确认此前误报为后台进程被回收导致，非代码问题
- 桌面壳 desktop/main.js：删除 ELECTRON_RUN_AS_NODE 环境变量（让本机 electron 正确启动）；开发态分支转发 renderer console-message 到 npm 终端（前端报错可见）
- 发布包隐私清理（suxiaomo-studio-v0.0.4）：对打包产物做只读敏感信息扫描——确认无 API Key/Token/数据库/.env 泄露，前端 .map 源映射为 0；发现 2 处低风险开发机路径残留（仅暴露开发者用户名与开发文件夹名，无用户数据路径）：① app.asar 内 main.js 注释含 `C:\Users\snail`、`F:\suxiaomo-studio-dev\workspace`；② backend/_python/Scripts/wsdump.exe（websocket-client 自带调试工具，app 不调用）shebang 含 `C:\Users\snail`。修复：main.js 删除 3 处注释里的开发路径；package.json extraResources backend filter 增加 `!**/wsdump*` 排除该 exe（dev 环境仍保留、仅不打包）。重新打包后包体干净
- 桌面壳 proxyApi 修复拖拽/中断场景下的 `ERR_STREAM_WRITE_AFTER_END` 弹窗：打包态 `/api` 反向代理在请求出错时，若响应已结束/销毁仍调用 `res.end()` 会触发主进程报错；改为先检查 `res.writableEnded || res.destroyed`，已结束直接返回；headers 已发送时改为 `res.destroy()` 中断连接，避免污染正在传输的视频流

## v0.0.3（2026-08-09）
【每日任务 / 首页提示 / 小说推文优化】
- 卡片三行结构：①操作软件（加粗，去掉「所属用户」展示，字段仍保留在 DB/表单）②任务摘要（浅蓝块 + 左侧主题色条，最多 3 行截断）③必做事情（橙色，单行截断）
- 顶部汇总条：总 / 已完成 / 已截止 / 未完成 四态 + 一键「全部完成 / 重置今日」
- 三态药丸：已完成（绿）/ 已截止（红）/ 未完成（灰），点击切换完成
- 截止日期相对化：今天截止 / 还剩 N 天 / 已逾期 N 天；无日期显示「⏰ 长期有效」；剩余天数数字按状态上色
- 暂停功能：status=active|paused 软隐藏；工具栏琥珀色「⏸ 已暂停 N 个」入口；单个恢复 + 暂停二次确认弹窗；「全部完成」排除暂停 + 逾期
- 跨天自动重置（60 秒检测，进入页面重置当日完成态）
- 详情弹窗：只留右上角 ×（圆形悬停红底）；去掉「所属用户」行；「必做事情」移至「其他账号」下方整宽块（长文换行）；网站链接旁「复制」改「复制链接」并跟在链接后
- 账号进度逻辑彻底删除（account_progress + required_accounts 全部逻辑、DB 列、schema 同步清理）
- 卡片 hover 操作按钮改绝对定位悬浮层（卡片高度恒定，hover 仅自身浮出，不联动整行）；提示词库卡片同步改悬浮层
- 卡片字体整体调大一号 + 间距 / 圆角优化；底部留白收紧（每日任务 padding-bottom 48px / 提示词库 44px）
- 首页「首次启动须知」弹窗：在「数据在 workspace 文件夹」项后新增「也可以选择自定义路径」说明，告知用户可在设置页选择自定义数据目录，后续升级直接沿用该目录
- 小说推文：卡片操作按钮（复制/编辑/删除）改绝对定位悬浮层，hover 才浮出、卡片高度恒定不联动整行，与每日任务一致；按钮文案「复制」改为「复制模板」；卡片「发布账号」由纯文本改为橙色圆角标签（acc-chip）展示，更醒目
- 仅前端 DailyTasks.vue / PromptLibrary.vue / Home.vue / NovelTweet.vue 改动（含暂停接口此前已落地），SFC 编译 0 错误

## v0.0.2（2026-08-08）
【提示词库 PromptLibrary 完整优化】
- 首页筛选：自定义风格 / 工具同步进下拉选项
- 卡片：移除封面图；标签颜色与详情页对齐（紫 = 风格 / 灰 = 工具）
- 类型统一两字：文本 / 图片 / 音频 / 视频 / 其他（新建 / 编辑 / 详情 / 卡片 / DB 全同步），并修正「文本」等标签背景色缺失
- 详情页布局：加宽至 820px；分类 / 类型 / #风格 / #工具 一行；二行 编辑·删除·一键复制；参考图与标签间加分隔线；首图 16:9
- 详情页操作：复制按钮反馈「已复制 ✓」；Prompt 正文支持选中复制、右键复制、划词浮出「复制选中」
- 滚动条美化（外层 modal + 正文内层）
- 设为首图：持久化到后端 set_cover；详情页 / 编辑页双向同步；编辑弹窗图片区「设为首图」底部通栏、删除按钮缩小 + 删除确认弹窗；首图角标放大
- 后端：上传图片文件名改为 pid+随机段（避免删除后重传覆盖同名文件）；裂图兜底（清理 DB 残留路径 + 前端 @error 占位）
- 导入：支持从 docx 提取提示词正文与图片入库（脚本 import_mj_docx.py，改 DOCX 路径可复用）
